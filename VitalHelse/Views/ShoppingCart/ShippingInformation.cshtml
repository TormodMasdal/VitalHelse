@using VitalHelse.Models.Enums
@model ShippingInformation

@{
    ViewBag.Title = "Sd";
    Layout = "_CheckoutLayout";
    ViewData["CheckoutStep"] = CheckoutStep.Cart;
}

<div class="list-group-item py-4 px-6">
    <div class="card">
        <div class="card-body">
            Du har ingen lagrede adresser. Legg til en ny adresse for å fortsette. <a href="#">Legg til ny adresse</a>
        </div>
    </div>
</div>


@Html.Partial("_AddAddressForm", Model)